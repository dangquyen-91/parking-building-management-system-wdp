services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pbms-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=production
